[pytest]
testpaths = tests
# Unit tests are the ones that need nothing running; the API tests need
# PostgreSQL. Marked so either half can be run on its own.
markers =
    api: exercises the HTTP layer against a real database
addopts = -ra --strict-markers
